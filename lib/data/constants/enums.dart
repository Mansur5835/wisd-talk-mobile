enum UserCases { NONE, LOGGED_IN, SIGNED_OUT }

enum SearchCase { searching, found, notFount, none }

enum ImageOption { camera, gellary }

enum ThemeMode { light, dark }

enum SelectedLang { UZ, EN, KO, NONE }

enum AddAnnouncementErrors { image, cotegory, none }

enum AddForumErrors { cotegory, none }
